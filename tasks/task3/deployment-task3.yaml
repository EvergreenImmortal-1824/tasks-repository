apiVersion: apps/v1
kind: Deployment
metadata:
  name: task3
labels:
    app: task3-app
spec:
  selector:
    matchLabels:
      app: task3-webapp
  template:
    metadata:
      labels:
        app: task3-webapp
    spec:
      containers:
      - name: task1-container
        image: jenishchhowala/tasks-repository:task3-v1
        resources:
          requests:
            memory: "100Mi"
            cpu: "250m"
          limits:
            memory: "300Mi"
            cpu: "500m"
        ports:
        - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: task3-app-service
spec:
  selector:
    app: task3-app
  ports:
  - port: 8002
    targetPort: 80
    type: NodePort
