apiVersion: v1
kind: Service
metadata:
  name: mongodb-svc
  namespace: database
spec:
  selector:
    app: mongodb
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    name: mongodb-port
